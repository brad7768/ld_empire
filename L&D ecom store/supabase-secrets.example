# Copy to .env.supabase.local (gitignored) or export before running deploy script.
# Never commit real keys.

# Stripe Dashboard → Developers → API keys → Secret key
STRIPE_SECRET_KEY=sk_test_

# Public site URL (no trailing slash) — used for Stripe success/cancel redirects
SITE_URL=https://ld-empire.ca

# After creating webhook in Stripe Dashboard → Webhooks
STRIPE_WEBHOOK_SECRET=whsec_

# Optional override (default: liwswmcofxlvlyokkazm)
# SUPABASE_PROJECT_REF=liwswmcofxlvlyokkazm
